name: Haskell CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Setup stack
      uses: mstksg/setup-stack@v1
      
    - name: Build
      run: stack build

    - name: Run tests
      run: stack test
